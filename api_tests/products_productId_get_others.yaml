# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=52a171b52e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_others.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Non-successful API scenarios"
    flow:
      # Test with incorrect URL
      - get:
          url: "/product/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 2

      # Test with incorrect productId
      - get:
          url: "/products/wrongProductId"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
          think: 2

      # Test with missing Authorization header
      - get:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401
          think: 2

      # Test with incorrect Authorization header
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer wrongToken"
          expect:
            - statusCode: 403
          think: 2

      # Test with valid productId but expecting incorrect response status
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 500
          think: 2
