# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=47d9fe845f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_others.csv"
    skipHeader: true
    fields:
      - "productId"

scenarios:
  - name: "Testing non-successful API responses"
    flow:
      # Testing a DELETE request with a non-existent product
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting a 404 Not Found error

      - think: 1

      # Testing a DELETE request with an invalid product ID
      - delete:
          url: "/products/invalidProductId"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Expecting a 400 Bad Request error

      - think: 1

      # Testing a DELETE request without authorization
      - delete:
          url: "/products/{{ productId }}"
          expect:
            - statusCode: 401 # Expecting a 401 Unauthorized error

      - think: 1

      # Testing a DELETE request with an incorrect API endpoint
      - delete:
          url: "/incorrectEndpoint/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting a 404 Not Found error

      - think: 1
