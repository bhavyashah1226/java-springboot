# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_put for http method type PUT in artillery framework

# RoostTestHash=6762b8ac71


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_put_others.csv"
    fields: ["id", "name", "description", "price"]
    skipHeader: true
  phases:
    - duration: 600
      arrivalRate: 5
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"
      auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Test scenario for non-successful API requests"
    flow:
      # Invalid payload - missing required fields
      - put:
          url: "/products/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1
      # Unauthorized request - missing authentication token
      - put:
          url: "/products/{{ id }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1
      # Invalid URL - non-existent endpoint
      - put:
          url: "/invalid_endpoint/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
          think: 1
