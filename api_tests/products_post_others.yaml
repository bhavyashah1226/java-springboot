# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products_post for http method type POST in artillery framework

# RoostTestHash=2c080099fd


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_post_others.csv"
    fields:
      - "id"
      - "name"
      - "description"
      - "price"
    skipHeader: true

scenarios:
  - name: "Test non-success API responses"
    flow:
      # Test scenario with invalid URL
      - log: "Testing with invalid URL"
        get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
        think: 1

      # Test scenario with missing required field in the payload
      - log: "Testing with missing required field in the payload"
        post:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
        think: 1

      # Test scenario with invalid authentication token
      - log: "Testing with invalid authentication token"
        post:
          url: "/products"
          headers:
            Authorization: "Bearer invalid_token"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 401
        think: 1

      # Test scenario with invalid payload data type
      - log: "Testing with invalid payload data type"
        post:
          url: "/products"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "invalid_id"
            name: "{{ name }}"
            description: "{{ description }}"
            price: "{{ price }}"
          expect:
            - statusCode: 400
        think: 1
