# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=52a171b52e


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_get_success.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "API requests to validate correct behavior with valid input payloads"
    flow:
      # GET /products/{productId}
      - get:
          url: "/products/{{ productId }}"
          headers:
            # Add the authorization token if it's required
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            # Expect a 200 status code
            - statusCode: 200
            # Assert the presence of response headers
            - hasHeader: "Content-Type"
            # Assert the presence of required properties in the response body
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "description"
            - hasProperty: "price"
          # Apply a "think" time after each request
          think: 1
