# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=47d9fe845f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "products_productId_delete_success.csv"
    fields:
      - "productId"
    skipHeader: true

scenarios:
  - name: "Delete Product API test scenario"
    flow:
      # First request to delete a product
      - post:
          url: "/products/{{ productId }}"
          headers:
            # assuming 'apiKey' as security schema name
            apiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - equals: 
                description: "Product deleted"
          think: 2
