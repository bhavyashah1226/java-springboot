# Test generated by RoostGPT for test api-spec using AI Type Open AI and AI Model gpt-4


# Test generated for /products_get for http method type GET in artillery framework

# RoostTestHash=ed0d766b50


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "products_get_others.csv"
    fields:
      - "productId"
      - "productName"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      # Send an GET request without authorization
      - get:
          url: "/products"
          expect:
            - statusCode: 401 # Expecting Unauthorized status code

      - think: 1

      # Send an GET request with incorrect URL
      - get:
          url: "/productss"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404 # Expecting Not Found status code

      - think: 1

      # Send an GET request with invalid productId
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Expecting Bad Request status code
            - hasProperty: "errors"
            - hasProperty: "message"

      - think: 1

      # Send a POST request with missing fields in payload
      - post:
          url: "/products"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            productName: "{{ productName }}"
          expect:
            - statusCode: 400 # Expecting Bad Request status code
            - hasProperty: "errors"
            - hasProperty: "message"

      - think: 1

      # Send a DELETE request with invalid productId
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400 # Expecting Bad Request status code
            - hasProperty: "errors"
            - hasProperty: "message"
